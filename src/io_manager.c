#include "network.h"
#include <stdio.h>

/*
 * Case configuration parser stub. Replace with full implementation in Phase 2.
 */

int load_case_config(const char *config_path) {
    (void)config_path;
    fprintf(stderr, "load_case_config() not implemented yet.\n");
    return -1;
}
